---
description: Review current staged/unstaged changes using the code-reviewer agent
---

Run `git diff HEAD` to get the current changes, then delegate to the **code-reviewer** agent to perform a thorough code review.

The review should cover: correctness, security issues, performance concerns, adherence to project conventions, and any bugs or edge cases. Provide actionable feedback with specific line references.
